services:
  ocr-bot:
    build: .
    container_name: ocr-bot
    restart: unless-stopped
    # Use Host Network to access LM Studio on localhost
    network_mode: host 
    env_file: .env
    volumes:
      - ./data:/app/data 